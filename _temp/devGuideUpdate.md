# DeveloperGuide.md update checklist

- [x] Add the standard document metadata and a table of contents so contributors can navigate the guide quickly.
- [ ] Add an acknowledgements section covering the project/template origin, JavaFX, JUnit, Jackson or other third-party libraries actually used, and the team’s AI-usage declaration where required.
- [ ] Add a setup/getting-started section that links to `docs/UserGuide.md` and documents the supported JDK (Java 25), Gradle commands, fat-JAR generation, CLI launch, JavaFX dashboard launch, and expected project layout.
- [ ] Expand the architecture section with a high-level component diagram using `docs/ClassDiagram.puml` (or add/update diagrams as needed), and explain the responsibilities and dependencies of the `domain`, `application`/CLI, `ports`, `infrastructure`, `presentation/UI`, `command`, `planner`, and `configuration` areas.
- [ ] Document the application bootstrap and dependency-composition flow from `timey.Timey` through `ApplicationFactory`, including which concrete adapters are injected for clocks, storage, location resolution, and transit planning.
- [ ] Add a presentation/UI component subsection with a class or component diagram and links to the relevant classes; explain `ConsoleUi`, `CommandLineApp`, `DashboardLauncher`/`TimeyDashboardApp`, `MainWindow`, `UiPart`, the FXML views, JavaFX-thread updates, background `Task` execution, and the shared immutable `DashboardState` flow.
- [ ] Add a command/logic component subsection describing command parsing, `CommandOptionParser`, each command family (`plan`, `choose`, `add`, `ls`, `rm`, `help`, `thx`), command-result/error propagation, and the stateful requirement that `choose` follows a successful `plan`.
- [ ] Add a model/domain component subsection describing `TimeyModel`, `PlanCommand`, `FixedCommute`, `RouteAlternative`/`RouteStep`, `SavedPlan`, `DepartureRecommendation`, and `RouteSelectionResult`, including pending-plan and selected-route state transitions.
- [ ] Add ports/infrastructure subsections showing the dependency inversion boundaries for `LocationResolver`, `TransitPlanner`/`RailTransitPlanner`, `FixedCommuteStore`, and `PlanStore`, and explain the OneMap, mock, in-memory, and file-backed implementations.
- [ ] Add a storage design subsection documenting `data/fixed-commutes.txt` and `data/plans.txt`, relative-path behavior, load/save formats, atomic temporary-file replacement, malformed-record tolerance, duplicate handling, and what state is preserved when a write fails.
- [ ] Add a dedicated implementation subsection for commute planning: location resolution, live OneMap rail-route lookup, route alternatives and route-step details, saved fixed timings being inserted as an option, and deterministic offline fallback behavior.
- [ ] Add a dedicated implementation subsection for departure calculation and route selection: arrival-time validation, personal buffer, leave-by calculation, `choose` validation/error states, the `LEAVE_NOW` case, and persistence of selected future plans.
- [ ] Add a dedicated implementation subsection for saved-plan lifecycle management, including startup loading, expired-plan pruning, stable ordering, duplicate prevention, dashboard next-plan selection, and cleanup on application close.
- [ ] Add a dedicated implementation subsection for HTTP reliability, documenting rate limiting, request timeouts, bounded retries/backoff, response/error mapping, and the user-safe recovery messages.
- [ ] Add a dedicated implementation subsection for dashboard command execution, documenting `DashboardCommandExecutionGate`, cancellation/overlap handling, loading/success/failure rendering, and why model/session access is serialized.
- [ ] Add design-consideration notes for major decisions, such as ports-and-adapters versus direct service dependencies, live routing versus deterministic offline estimates, atomic file replacement, injected clocks, and CLI/dashboard sharing the same command session.
- [ ] Add a documentation, logging, testing, configuration, and dev-ops section describing where logs are written, how `ApplicationConfiguration` controls paths/time zone/live-data endpoints, how to run checks, and how to diagnose live-service failures.
- [ ] Strengthen the testing policy with a test-architecture map covering parser, command, domain, planner, infrastructure, storage, UI helper, FXML-resource, and concurrency tests; list the existing verification command and explicitly state that live internet access is not required because request doubles and fixtures are used.
- [ ] Add a requirements appendix with the product scope: target users, supported planning/saved-timing/saved-plan/dashboard capabilities, live-data and offline behavior, persistence guarantees, and explicitly out-of-scope or planned integrations.
- [ ] Add user stories for the core workflows, such as planning a commute by arrival time, comparing routes, choosing a route, receiving a leave-by recommendation, saving/reusing/removing fixed timings, viewing upcoming plans, using the CLI, and recovering from unavailable live data.
- [ ] Add use cases with main success scenarios and extensions for invalid/missing options, past arrival times, unknown locations, no live routes, no pending plan, invalid route numbers, duplicate saved timings, expired plans, storage failures, and cancellation/failure of background dashboard work.
- [ ] Add non-functional requirements covering supported operating systems/JDK, responsiveness and bounded network waits, local-data handling, deterministic behavior without live services, accessibility/usability of the CLI and dashboard, and safe recovery from malformed or unavailable external data.
- [ ] Add a glossary defining terms used in the guide, including commute, fixed timing, saved plan, route alternative, route step, transfer, personal buffer, leave-by time, pending plan, fallback/offline estimate, session, and OneMap.
- [ ] Add an appendix of manual-testing instructions for CLI launch/shutdown, JavaFX dashboard launch and layout, each supported command, route selection and departure recommendations, fixed-timing CRUD, saved-plan listing/pruning, live-success and offline-fallback flows, invalid input, malformed storage files, and concurrent/cancelled dashboard requests.
- [ ] For each manual-test area, provide prerequisites, exact sample commands/data, expected output, and edge cases; keep the detailed command syntax in `docs/UserGuide.md` and link to it instead of duplicating the entire command reference.
- [ ] Add a planned-enhancements appendix that reflects the current status list and separates future items such as calendar/virtual-event/weather/LTA integrations, notifications, cached routes, walking-speed preferences, and fuller saved-location management from features already implemented.
- [ ] Replace the placeholder acknowledgements text in the current guide with completed project-specific acknowledgements before the guide is considered complete.
